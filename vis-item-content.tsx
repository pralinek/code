.vis-item-content {
    width: 100%;       /* Ensures the content fills the full width of the item */
    height: 100%;      /* Ensures the content fills the full height of the item */
    white-space: nowrap; /* Prevents text from wrapping, optional based on your needs */
    overflow: hidden;  /* Hides any overflow, optional based on your needs */
    display: flex;     /* Flexbox to help center content, if needed */
    justify-content: center; /* Horizontally center content, if desired */
    align-items: center; /* Vertically center content, if desired */
}